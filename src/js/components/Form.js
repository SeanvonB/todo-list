// Form.js

/**
 * Factory that creates DOM elements for input form modals
 *
 * @param {boolean} editMode
 * @returns {HTMLFormElement}
 */
export const Form = (editMode = false) => {
	const template = document.createElement("form");
	return template;
};
